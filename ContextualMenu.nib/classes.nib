{
    IBClasses = (
        {
            ACTIONS = {
                addToWhitelist = id; 
                copyFlashMovieURLToGeneralPasteboard = id; 
                editWhitelist = id; 
                loadFlash = id; 
                removeFromWhitelist = id; 
            }; 
            CLASS = CTFClickToFlashPlugin; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSView; 
        }
    ); 
    IBVersion = 1; 
}